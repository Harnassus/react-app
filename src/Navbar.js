import logo from './image/Vector.png'

const Navbar = () => {
    return (
        <nav className='nav'>
            <img alt="logo" src={logo} className='nav-logo' />
        </nav>
    )
}

export default Navbar;