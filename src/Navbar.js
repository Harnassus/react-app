

const Navbar = () => {
    return (
        <nav className='nav'>
            <img alt="logo" src={process.env.PUBLIC_URL + "/image/Vector.png"} className='nav-logo' />
        </nav>
    )
}

export default Navbar;